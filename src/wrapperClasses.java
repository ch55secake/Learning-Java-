public class wrapperClasses {
    public static void main(String[] args) {
        // wrapper
        // ===========
        //Boolean
        //Character
        //Integer
        //Double
        //wrapper classes means you can use useful methods with data types like below
        //however they can still be treated as normal and used as normal
        String x = "Pikachu";
        String newX = x.replace("i", "a");
        System.out.println(newX);
    }
}
